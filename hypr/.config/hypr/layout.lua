hl.config({
    general = {
        layout = "master",
    }
})

hl.config({
    dwindle = {
        preserve_split = true,
    },
})

hl.config({
    master = {
        new_status = "master",
        orientation = "right",
        mfact = 0.60,
        new_on_top = false,
        focus_master_on_close = true
    }
})
